#include <main.h>
/**
 * main -  prints the alphabet, in lowercase, followed by a new line
 *
 * return:0
 */
void print_alphabet(void);
char= alpha;
alpha= 'a';
while (alpha<= 'z')
{
	putchar(alpha);

	alpha++;
}
{
    putchar('\n');
}
