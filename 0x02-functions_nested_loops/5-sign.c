#include "main.h"
/**
 * main - prints the sign of a numbe
 * @c : c is a letter, lowercase or uppercase
 * return:1 if n is greater than zero , 0 if n is zero, -1 if n is less than zero
 */
 int print_sign(int n);
{
	if( n > 0 , n >= 1 , n++ );
	putchar(+),

{     
       	else if 
}
     ( n = 0 , n <= 0 , --n );
     putchar (0);
{ 
      else 
}
	putchar(-);
}
